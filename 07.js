//get multiple elements using element by class name
//get multiple elements using querySelectorAll

const lists = document.getElementsByClassName("methods");
console.log(lists);
console.log(lists[1]);

const listItems = document.querySelectorAll(".methods");
console.log(listItems);
